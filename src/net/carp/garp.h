#ifndef __GARP_H__
#define __GARP_H__ 1

/*
 * We transfered declarion to oss_carp.h so it is visible as an API call
 * int gratuitous_arp(const int dev_desc_fd);
 */
#include "OSS/Net/oss_carp.h"

#endif
